export default interface gig {
    title: string,
    avatar: string,
    thumbnail: string,
    rating: Number,
    category: string,
    price: Number,
    description: string,
    creator: string
}